{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug GTK App",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/server.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "Path",
                    "value": "C:/msys64/mingw64/bin;${env:Path}"
                }
            ],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Build GTK App"
        }
    ]
}
